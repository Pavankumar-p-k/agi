{
  "name": "@jarvis/sdk",
  "version": "0.1.0",
  "description": "JARVIS Agent OS — typed SDK for all frontends",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "node scripts/generate-types.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.4.0",
    "vitest": "^4.1.9"
  }
}
