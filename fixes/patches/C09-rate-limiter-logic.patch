From: JARVIS Audit Bot
Date: 2026-06-09
Subject: [PATCH C-09] Fix rate limiter exempt-path logic inversion

High: Rate limiter in main.py has inverted logic — it rate-limits
the exempt paths (/health, /docs, etc.) and exempts everything else.

File: core/main.py

--- a/core/main.py
+++ b/core/main.py
@@ -109,7 +109,8 @@
     async def rate_limit_middleware(request: Request, call_next):
         client_ip = request.client.host if request.client else "unknown"
         exempt = ("/health", "/docs", "/openapi.json", "/redoc", "/static")
-        if not request.url.path.startswith(exempt):
+        exempt_path = any(request.url.path.startswith(e) for e in exempt)
+        if not exempt_path:
             if not await rate_limiter.check(client_ip):
                 return JSONResponse(status_code=429, content={"error": "Too Many Requests"})
         response = await call_next(request)
